Examples

Scrape a single page and print JSON:

  bun run src/cli.ts --url https://example.com/specials --site crumbl

Save results to a file:

  bun run src/cli.ts --url https://example.com/specials --site crumbl --out data/today.json
